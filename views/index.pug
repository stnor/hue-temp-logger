extends layout

block content
  section.sensors
    each s in tempSensors
      div.sensor
        div.sensor-name= s.sensorName
        div.sensor-temp
          span= s.temperatureC
          span °
        div= s.obstime
